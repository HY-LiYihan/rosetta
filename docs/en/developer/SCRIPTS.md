# Scripts

Last updated: 2026-05-08

The unified CLI entrypoint is:

```bash
python scripts/tool/rosetta_tool.py
```

Use scripts for reproducible workflow runs, experiments, exports, deployment checks, and operational tasks. Prefer the current workflow wrappers over legacy `research` or `corpusgen` entrypoints when adding new behavior.

Canonical Chinese reference: [Scripts](../../developer/SCRIPTS.md).
